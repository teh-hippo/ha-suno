download_library:
  name: Download library
  description: Trigger a download of your Suno library to the configured directory.
  fields:
    force:
      name: Force
      description: Re-download all files, ignoring download state.
      required: false
      default: false
      selector:
        boolean:
